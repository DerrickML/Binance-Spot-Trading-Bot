---
trigger: always_on
---

## Terminal behavior rules
- Use terminal commands actively to inspect, install, test, and verify
- Check for existing project tooling before adding new tooling
- Use standard, safe commands
- Do not assume dependencies are installed
- After scaffolding or major edits, run verification commands
- Fix obvious import, type, and syntax errors before stopping
