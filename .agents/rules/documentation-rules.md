---
trigger: always_on
---

## Documentation rules
- Keep README updated when capabilities change
- Keep architecture docs aligned with the codebase
- Document setup, configuration, safety boundaries, and usage commands
- When a module is added, its purpose should be obvious from naming and structure
