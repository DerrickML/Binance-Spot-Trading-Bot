---
trigger: always_on
---

## Logging and observability rules
- Use structured logging
- Log startup, shutdown, mode, signals, risk decisions, orders, fills, errors, and halts
- Important runtime actions must be human-readable
- Error logs must be specific enough to support debugging
- Do not swallow exceptions silently