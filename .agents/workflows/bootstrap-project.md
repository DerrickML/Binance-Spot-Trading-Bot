---
description: Bootstrap the trading bot project foundation in the current repository.
---

Bootstrap the trading bot project foundation in the current repository.

Tasks:
1. inspect the repo first
2. create or align the project structure for a production-structured Python trading system
3. create `pyproject.toml`
4. create `.env.example`
5. create core package directories and `__init__.py` files
6. implement initial config, logging, database bootstrap, and CLI foundations
7. install dependencies using the existing project tool if present, otherwise use a clean standard approach
8. run a basic verification command to confirm imports and CLI startup work

Requirements:
- preserve existing work
- use Python 3.12+
- use pydantic, SQLAlchemy, pytest, httpx, pandas, numpy, python-dotenv
- keep live trading disabled by default

Output:
- files created or modified
- commands run
- verification result
- next best step